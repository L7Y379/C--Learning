#include <iostream>
#include<string.h>
using namespace std;

class Person
{
   protected:
     string name;
     int age;
     char gender;
     static int counter；     // 统计总人数
public:
     Person();   
     Person(string na, int , char){//通过参数初始化成员
     		
     }
     ~Person();
     void set(string, int, char);   //设置人员基本信息
     void show();             //显示人员基本信息
     int get_counter();          //获取人员总数     
};

int main(){
	return 0;
} 
